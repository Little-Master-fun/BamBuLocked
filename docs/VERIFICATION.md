# 当前验证记录

已在 macOS arm64 完成：

- .NET SDK 8.0.424，Release 解决方案编译成功，0 警告、0 错误。
- 核心自动测试 29 项全部通过：CAS 成功身份提取、命名空间前缀变化、拒绝失败/缺字段/重复身份/DTD、凭据表单编码、票据清理、HTTPS、保留工程所有权、日志写入失败时不授权/撤销授权、幂等结束和异常恢复；新增组织按会话保存、必填/长度校验和旧数据库迁移。
- Windows x64 self-contained 发布成功，包含 WPF/运行时依赖；无需目标电脑另装 .NET。
- 5 个 PowerShell 脚本通过官方 PowerShell 解析器语法检查。
- 使用虚构 Windows SID 运行白名单生成器，检查 XML 集合和 AuditOnly 模式；没有在当前系统应用任何注册表或系统策略。

未执行：Windows GUI 实际运行、注册表部署/恢复、AppLocker 强制执行、真实校园认证、打印机传输/打印、多屏/锁屏恢复/防绕过。构建通过不能代替这些测试。

见 WINDOWS-DEPLOYMENT.md 的现场验收表。

登录页样式更新：已重新编译并发布 Windows 包，0 警告/错误；3 个原项目 PNG 作为 WPF Resource 嵌入。浏览器样式预览已检查插画、圆角输入框、组织字段及按钮；WPF 实际运行仍待 Windows 验证。本次未修改认证/日志规则，未增加与布局实现重复的单元测试。

录屏版本新增 7 项存储测试：优先删除最早、逐段删除到空间足够、七天过期、保护活动及无关文件、空间仍不足、拒绝路径越界、崩溃恢复。真实 Windows 录屏、匿名管道、Job 退出和画面完整性尚未执行。FFmpeg 不随包分发，提供下载校验安装脚本。

管理员管理页版本：核心测试合计 37 项通过，新增 8 项覆盖只读查询不创建/迁移数据库、不结束会话、筛选统计/分页、日期边界、全量 CSV 与公式防护、录像关联和打开前重新检查。Release 编译及 Windows x64 发布成功，0 警告/错误；所有 PowerShell 脚本（含新增 Start-Admin.ps1）通过解析。浏览器演示预览已检查布局和数据初始渲染。原生 WPF 表格、管理员 UAC、播放器调用仍需按 ADMIN.md 在 Windows 实机验收。

人员频次和精确时间查询版本：核心测试合计 39 项通过。新增测试覆盖按学号聚合、人员分页、精确身份下钻（001 不匹配 0012）、保留筛选条件、某一秒的跨时段会话匹配及一致导出。Windows x64 发布成功。新增人员窗口、时分秒输入和时间结果弹窗尚未在 Windows 实机运行。

校园管理员入口版本：41 项核心测试通过，新增空名单拒绝、精确认证学号匹配、拒绝姓名匹配及近似学号测试。Release 编译及 Windows x64 发布成功。管理员在原认证桌面显示管理窗口，不进入打印会话；需在 Windows 验证 CAS 分流、模态子窗口级联关闭、管理员鼠标超时、系统锁定及内置 MKV 播放支持。
